import { Domain } from "cqrs";
import EventStore from "cqrs/src/EventStore";
import { Router, RequestHandler } from "express";
export interface Option {
    baseUrl?: string;
    createInterceptor?: RequestHandler | RequestHandler[];
    callInterceptor?: RequestHandler | RequestHandler[];
    actorPath?: string;
    listenerPath?: string;
    eventstore?: EventStore;
}
export default function createMiddel(opt: Option): {
    router: Router;
    domain: Domain;
};
